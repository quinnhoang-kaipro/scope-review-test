// This UI is currently static (design-only). Keep the script minimal
// so the plugin UI doesn't error due to missing demo inputs.

document.addEventListener('DOMContentLoaded', function () {
  // Intentionally left blank for now.
});
